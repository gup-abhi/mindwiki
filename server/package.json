{
  "name": "mindwiki-server",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "../node_modules/.bin/eslint src --no-ignore --max-warnings 0"
  },
  "dependencies": {
    "@tsndr/cloudflare-worker-jwt": "^3.2.3",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.3.3",
    "wrangler": "^3.78.0"
  }
}
